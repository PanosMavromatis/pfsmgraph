# fix/hmm-viterbi-log2

**Status**: active
**Created**: 2026-09-14
**Subgoal**: Unify the logarithm across the Viterbi backends — `docs/plan/TODO.md`,
revision 03-hmm-v0.2.0

## Goals

- [x] Confirm the premise before changing anything
  - [x] Run `/dp-compile:phase-check viterbi`. Expect all of `formalization`, `python`, `cython`, `cpu_parallel` and `cuda` fresh, with the chain `_viterbi.py` → {`FORMALIZATION.md`, `_viterbi_cython.pyx`} → `_viterbi_cpu_parallel.py` → `_viterbi_cuda.py`
    > **Q:** Has `FORMALIZATION.md` been reviewed and approved, and does it stand as the contract this branch works against, including its open "Which host `log2` is contract" paragraph, which goal 2 resolves?
    > **A:** Approved.
    > **Done:** premise confirmed 2026-09-14. Nominal phase **4**; all five phases **fresh**, graph recovered and branching at the root. Recorded hashes, each computed with the target's own header line removed: `_viterbi.py` `6b26469b…` (named by both `FORMALIZATION.md` and `_viterbi_cython.pyx`), `_viterbi_cython.pyx` `79da7c8d…`, `_viterbi_cpu_parallel.py` `5f45abb4…`. `test_viterbi.py`: 89 passed, 0 skipped, header `backends: python ✓ · cython ✓ · cpu_parallel ✓ · cuda ✓` on an NVIDIA L4. All three `.vpath.xls` oracles exercised.
    > **Note:** only `_viterbi.py` lacks a provenance header, correctly: it is the root. Goal 3 should expect **stale then blocked**, not all-stale: editing the `.pyx` leaves `_viterbi_cuda.py` blocked until `_viterbi_cpu_parallel.py` is re-stamped.
  - [x] Measure the numpy-vs-libm `log2` disagreement rate on this host rather than taking DEFERRED's AVX-512 figure (3,937 in 4,000,000) on trust, and find a concrete model whose `total_bits` differs between phase 1 and phases 2-3
    > **Done:** reproduced 2026-09-14 on a 4-vCPU Xeon @ 2.20GHz with AVX-512F/BW/DQ/VL, numpy 2.4.6, numba 0.67.0. Phase 1's `bits` of a contiguous `(S, S)` array takes numpy's SIMD `log2`; numba-scalar `np.log2`, ctypes libm `log2` and `math.log2` agree with one another exactly (0 of 200,000), so phases 2 and 3 share libm's result. Every disagreement is exactly **1 ulp**. The phase-4 test's own generator and seed (`_generated_models(default_rng(20260913), 200, 9)`) reaches it in **1 of 200** models: **model #82, `S=7`, 5 user symbols, `N=16`**, phase 1 `63.37898478021349` against phases 2 and 3 `63.378984780213486` (−1 ulp), states equal. That is the model DEFERRED describes as "7-state, 16-symbol"; the 16 is the sequence length, not the alphabet.
    > **Note:** **the rate is a property of the input distribution, not a host constant**, so DEFERRED's "about 0.1%" should not be quoted without its inputs. Here: 7,763 of 4,000,000 (0.19%) for uniform `x`, 2,872 of 4,000,000 (0.07%) for kernel-shaped `x = u₁·u₂`. By range of `x` the rate climbs from 0.013% below 0.001 to **0.64% in `[0.9, 1)`**. Disagreements concentrate on high-probability arcs, which are the ones a Viterbi path is made of.
    > **Note:** **arc-level disagreement rarely reaches `total_bits`, which is why the existing seeds pass.** A separate sweep of 300 generated models (`S` 2-8, `A` 2-16, `N` 1-59) found 0 differing totals and 0 differing paths across all three later backends. A one-ulp error in a few-bit arc cost is usually lost to rounding when added to a `delta` tens of times larger. So a test that samples models will almost never exercise the split. Goal 4's near-tie has to be **constructed**: choose arc probabilities whose `log2` is known to disagree, placed where the two candidates meet. Relying on a lucky seed would repeat the mistake the existing exact assertions already make.
- [x] Choose the fix and state which `log2` is contract
  > **Q:** Which fix: (a), one numpy table, with the contract "every arc cost and the seed are `bits()` on the host (numpy's `log2`), and kernels perform only `+` and `<`", or (b), libm everywhere?
  > **A:** (a), the numpy table.
  > **Done:** **The contract is numpy's `log2`, evaluated on the host through `bits()`, for every arc cost and for the seed; a kernel performs only `+` and `<` on the resulting bits.** Phases 2 and 3 adopt phase 4's host table: `_decode` receives `arc_bits (S, S, U)` over `U = np.unique(codes)`, the re-indexed `sym`, and `seed = bits(init_p)`. The public `_viterbi(init_state_p, transition_p, output_p, codes)` signature is unchanged, as ADR 0003 requires. `_viterbi.py` is not edited, so `FORMALIZATION.md` stays fresh and goal 3's chain is `.pyx` → `cpu_parallel` → `cuda`, re-stamped one link at a time.
  > **Note:** **numpy's `log2` is a function of the value alone on a given host**, which is the premise (a) rests on. Measured 2026-09-14 over 1,000,003 kernel-shaped inputs, numpy 2.4.6 dispatching `X86_V4`: 0 differences between one long contiguous call and 0-d scalar calls, 49-element (7×7) calls, call lengths 1-17, a `::3` strided view, a transposed view, or a shifted alignment. So phase 1's per-position `(S, S)` and a per-record `(S, S, U)` table produce identical bits.
  > **Note:** **the agreement this buys is within one host, not across hosts.** numpy's result depends on its SIMD dispatch (`X86_V3`, `X86_V4`, ARM) and libm's would depend on the glibc version, so neither option could promise bit-identical output across machines. `FORMALIZATION.md` must state the scope as "all backends agree on the same host", not as cross-machine reproducibility.
  > **Note:** **the table works only because Viterbi's recurrence is pure min-plus after the logarithms.** Revision 03's forward pass needs `log`/`exp` inside the recurrence for log-sum-exp, which no host table can supply, so the question returns there in another form. This decision covers the decode only.
  - [x] (a) One numpy table for every backend, as phase 4 builds it: costs for the distinct symbols in the record, `(S, S, |present|)`. Weigh this against phase 1's refusal of the `(S, S, A)` precompute as `O(S²·A)`. The per-record gather is bounded by the symbols used, not the alphabet, but it is still a table, and phases 2-3 would stop computing `log2` in their inner loops
    > **Note:** chosen. The weighing phase 4's docstring already recorded holds: `(S, S, A)` was refused as `O(S²·A)` host work that reads like ADR 0015's forbidden emission hoist, whereas `arc_bits[i, j, k]` still depends on both endpoints and is still read inside the inner loop; only where the logarithm runs moves. Host memory is `8·S²·U` bytes per record, `U ≤ min(N, A)`: about 10 MB at `S = 160, U = 50`, 205 MB at `U = 1000`. Expected to speed phase 2 up rather than slow it, since core.md attributes Cython's 222 ms at `S = 160` to its ~10⁷ scalar libm calls. That is a prediction, not a measurement.
  - [-] (b) libm everywhere: route phase 1 through scalar `log2`, and phase 4's host table with it. This loses numpy vectorisation in phase 1
    > **Descoped:** not chosen. It edits the oracle to match two derived backends, which makes the formalization and all of phases 2-4 stale for a defect confined to phases 2 and 3. Phase 1 would need either S² Python-level `math.log2` calls per position or a numba dependency the reference kernel must not have, and phases 2-3 would keep libm in their inner loops.
- [x] Make the change in the backends and regenerate the provenance chain
  > **Q:** Build the arc-cost table (ii) by copying phase 4's three lines into each wrapper, or (i) in a shared helper module the three kernels import? And may the phase 2/3 docstring corrections land in this goal rather than goal 5?
  > **A:** Recommendation accepted: (ii), a copy in each wrapper, and the docstrings are corrected here. (ii) keeps every line that carries the contract inside a file the provenance graph tracks; an untracked helper, like `_numeric.py` today, could change with no kernel going stale. Correcting the docstrings in goal 5 instead would change both kernels' hashes a second time and force a second re-stamp of the chain.
  - [x] Edit the kernels the chosen fix touches. Keep the operation order (multiply, `-log2`, add) and the first-wins ascending reduction unchanged
    > **Done:** 2026-09-14. `_viterbi_cython.pyx` and `_viterbi_cpu_parallel.py` now take `_decode(seed, arc_bits, sym, delta, psi, states)`. Their wrappers build `seed = bits(init)`, `present, sym = np.unique(codes, return_inverse=True)` and `arc_bits = bits(trans[:, :, None] * out[:, :, present])`, which are phase 4's lines, so each kernel performs only `+` and `<`. The `prange` axis, the serial ascending `i` loop, strict `<` and the backtrace are untouched. The public `_viterbi(init_state_p, transition_p, output_p, codes)` signature is unchanged. Docstrings were corrected in the same edit, each with a dated *Corrected 2026-09-14* note; `_viterbi_cuda.py` got a docstring-only *Resolved* note. After `uv sync`: `test_viterbi.py` 89 passed. **Model #82 of the phase-4 generator, the one that was −1 ulp, is now `63.37898478021349` on all four backends, and 0 of 200 models differ from phase 1 on any backend.** That is the check that the `.so` was actually rebuilt (`packages/pfsmgraph-hmm/build/cp312/`), since the old libm kernel passed the suite too.
    > **Note:** that `uv sync` also reported `pfsmgraph-hmm==0.1.0.dev0 → 0.1.0`. The environment's installed metadata predated the 0.1.0 release commit and was refreshed by the rebuild. Nothing in this branch changes a version.
  - [x] Regenerate every `dp-compile: derived-from` header downstream of an edited file, including `_viterbi_cuda.py`'s even if its body is unchanged, and `FORMALIZATION.md`'s `Derived from` hash if `_viterbi.py` changes
    > **Done:** re-stamped in dependency order, each hash computed with the target's own header line removed. `_viterbi_cpu_parallel.py` now records `_viterbi_cython.pyx` at `sha256:923163bb…` (was `79da7c8d…`). `_viterbi_cuda.py` then records `_viterbi_cpu_parallel.py` at `sha256:7d961367…` (was `5f45abb4…`). `_viterbi.py` was not edited, so `FORMALIZATION.md` and the `.pyx` header still record `6b26469b…` and match. All four recorded edges now match their sources. Full suite: **319 passed**.
    > **Note:** before re-stamping, **both** `_viterbi_cpu_parallel.py` and `_viterbi_cuda.py` were stale, not stale-then-blocked as goal 1's note predicted. That prediction assumed only the `.pyx` would change. Here phase 3's body changed too, so phase 4's recorded hash stopped matching at once. The phase-detection rule still applies: stamping phase 3 first is what made stamping phase 4 meaningful.
- [ ] Tighten the tests
  - [ ] Replace phase 4's one-ulp-per-arc bound against phases 2-3 with exact equality
  - [ ] Construct a near-tie whose two candidate costs fall in the disagreeing inputs, and assert all four backends choose the same predecessor
- [ ] Update the documentation
  - [ ] `FORMALIZATION.md`: name which `log2` is contract, replacing "remains open"
  - [ ] Correct the bit-exactness claims in the phase 2 and 3 docstrings, and phase 4's paragraph about the split
  - [ ] Remove the entry from `DEFERRED.md` and the "not bit-identical" caveat from `core.md`
