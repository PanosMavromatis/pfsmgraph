# feat/hmm-convergence-backend

**Status**: active
**Created**: 2026-09-17
**Subgoal**: Settle how the topology search uses the compiled backends, and accept ADR 0024 — `docs/plan/TODO.md`, revision 04-hmm-v0.3.0

## Context

[ADR 0024](../../design/adr/0024-search-compiled-work.md) was drafted 2026-09-17 at Proposed. This work comes **before the `docs/api/` audit** because it may add a public keyword to `baum_welch`, and before the training log because that goal designs around `baum_welch(..., log=)`. Three questions were asked after PR #48: does the search layer need compiling (no, ADR 0024 §1); how Cython relates to the parallel phases in a full search (they are alternatives chosen per call by name, not layers, and all but `torch` are bit-identical; §2 and §4); and should parallelizing the search be recorded for later (yes, along trials; §3). The profile that answered the first is the Note under goal 4, and it found the round's real cost: `baum_welch`'s convergence check runs the numpy forward pass whatever `backend=` says (`_baum_welch.py:302`, `_finish` at `:320`), which ADR 0023 §7's `score_backend=` could not reach. This **qualifies the release goal's expectation** from `HMMLIB-ACCOUNT.md` §12, that leaving the driver interpreted "costs little": true of the driver, which was running the one uncompiled forward pass.

The nine goals below are the master plan goal's acceptance criteria, moved here verbatim when the branch opened, so that this file is the whole target of `/hitl-step`.

## Goals

- [x] Decide ADR 0024's Open item: **(a)** a separate `score_backend=` on `baum_welch`, default `"python"`, validated before any work, mirroring the trials; or **(b)** derive the check's phase from `backend=`, `"python"` under `"torch"`, with no signature change but an implicit mapping at odds with ADR 0021's no-substitution rule. The ADR notes that (a) has the precedent. Record the answer in the ADR and mark it Accepted in the record and its `adr/README.md` row.
  > **Q:** How should `baum_welch`'s convergence check name its `forward_backward` phase: (a) a separate `score_backend=` keyword, default `"python"`, validated before any work and forwarded by the trials and `_search`; or (b) derived from `backend=`, with `"python"` under `"torch"`?
  > **A:** (a) `score_backend=`. The trials already carry the keyword, but `_re_converge_and_score` (`_trials.py:167`) passes it only to `_scan_d`, so under (a) they forward a name they already hold. `_resolve("forward_backward", "torch")` already raises `ValueError` ("has no 'torch' backend"), so refusing the torch phase needs no new check.
  > **Done:** ADR 0024 is Accepted. §2 names `score_backend=` and its torch refusal, the signature cost is now definite, (b) is argued under Alternatives considered, and a new Resolved section records the decision; the other Open items stay. `adr/README.md`'s row and narrative agree. `core.md`'s "0024 is Proposed" sentence was corrected by `/smart-commit`'s docs sync in `18c171d`, not left for goal 9.
- [x] Implement it: the check at `_baum_welch.py:302` and `_finish` (`:320`) take the chosen phase; `_re_converge_and_score` (`_trials.py:165-173`) and `_search` (`_search.py:174`) pass their `score_backend` through. Rewrite the contract sentence in `BaumWelchResult`'s docstring ("the check at the end of each batch, and so the last entry, is the reference forward pass on every backend", added in `58942fe` as a description, never argued) and the comments at `_trials.py:170` and `_search.py:175` that rely on it. Adds no kernel and no backend row, so the `dp-compile` gate does not arm and the backend header is unchanged.
  > **Note:** `_search.py:175` carried no comment to rewrite; it is the bare `data_bits = converged.description_lengths[-1]`, so `_search` only forwards `score_backend` at `:174`.
  > **Note:** `baum_welch` validates the check's phase as `_resolve("forward_backward", score_backend, "baum_welch's score_backend")`. The label matters: plain `"baum_welch"` would report "baum_welch has no 'torch' backend", which is false, since its E-step has one. `_finish`, the check on the `max_cycles` path, takes it too.
  > **Note:** Two existing spies in `test_trials.py` failed, as they should: they pin the exact keyword set `_try_split` and `_try_merge` pass to `baum_welch`. Both now pass `score_backend="cython"`, a non-default value, because with `"python"` a trial that dropped the keyword would still pass. The merge spy also pins the default. The split spy is renamed `test_backend_score_backend_batch_size_and_floor_reach_baum_welch`.
  > **Done:** `baum_welch(..., score_backend="python")`, checked before any work, reaches both convergence checks; `_re_converge_and_score` and `_search` forward it. The `BaumWelchResult` contract sentence, the `_trials.py:170` comment and the three `score_backend` docstrings now describe the check. Suite: 2916 passed over two runs, the hmm package and the root tests, `test_trials.py` re-run whole after the spy fixes.
- [x] Test it: `BaumWelchResult` (params, `description_lengths`, `cycles`, `converged`, `degenerate_states`) is `==` with the check on each available `forward_backward` phase against `"python"`; a trial's and a search's results are `==` likewise; an invalid or `torch` phase for the check is refused before any work if (a) is chosen; and the default leaves every existing test and executed output unchanged.
  > **Note:** Equality alone cannot see the keyword being dropped, since every phase returns the same bits, so each `==` test has a spy beside it: `_mdl._resolve` must see exactly `("forward_backward", score_backend)` once per check (`cycles // BATCH_CYCLES` converged, `1` at `max_cycles=7`, where `_finish` makes the only check), and `_search`'s start `baum_welch` must receive `score_backend`. Mutation-checked: dropping the phase at the batch check failed the spy's converged cases on cython, cpu_parallel and cuda (3); in `_finish` its `max_cycles=7` cases (3); removing the up-front `_resolve` failed all four refusal cases and the no-fallback case (5); dropping `_search.py:174`'s forwarding failed the start spy (1). No `==` test failed on any mutant, as expected.
  > **Note:** The shared `score_backend` fixture reads `_status("forward_backward")`, not `backends()`, which answers only for public calls and raised `ValueError` for `forward_backward` on first run.
  > **Note:** The default is shown unchanged by the existing suite rather than a new test: every pre-existing test, `test_api_docs.py`'s executed blocks included, passed at goal 2 and again here.
  > **Done:** 98 tests. `conftest.py` gains a `score_backend` fixture over `forward_backward`'s table. `test_baum_welch_backends.py`: `==` runs on every phase for 8 generated trainings and `m008` at `max_cycles` `None` and `7` (72), the per-check spy (8), torch EM with each phase checking it (4), refusal of `torch` and `numpy` before any E-step under `python` and `torch` (4), and an unavailable check phase raising `BackendUnavailableError` with the E-step's own Cython row available (1). `test_trials.py`: both trials `==` their fixtures per phase (4). `test_search.py`: a two-round cython search `==` per phase (4) and the start spy (1). Whole suite 3088 passed (2990 + 98) on a host with cuda and torch.
- [x] Measure it: track the profiler as `.scratch/hmm-lush/measurements/search_round_profile.py` (same setup as the Note below), re-run the round after the change, and replace ADR 0024's "about 2 s, not yet measured" with the figure.
  > **Note:** Profile of one `_suggest_move` round (2026-09-17, 4-vCPU host, `m001_0005_005`, corpus as one record, `backend="cython"`, `score_backend="cython"`, `split_min_cycles=200`, two split trials per state, seed `SeedSequence(1, spawn_key=(1, 0))`): 20 moves in **12.7 s**. The numpy `_forward_backward` took **10.6 s** cumulative over 236 calls (84%), of which `safe_divide` 6.3 s over 606,622 calls, one per timestep, most of it `broadcast_shapes` and allocation rather than arithmetic. `_corpus_step`, the Cython E-step with its count sums, took 1.15 s; the search, surgery, `HMMParams` validation and `_scan_d` shared the remaining ~1 s. This is the second numpy forward pass found hiding in the scoring path, after `_suggest_d`'s 53-62% (`merge_round_cost.py`); threading `backend=` through the trials could not reach this one, because `baum_welch` makes the call itself. The script that produced it was an untracked session scratch file, so this goal rewrites it rather than copying it.
  > **Note:** Measured 2026-09-17 by `search_round_profile.py` (4-vCPU Xeon @ 2.20 GHz, Python 3.12.3), before and after in one process, "before" rebuilt by patching `_baum_welch._corpus_description_length` to ignore its phase. Before: 9.38 s unprofiled, 12.65 s under `cProfile`, numpy `_forward_backward` 10.63 s over 236 calls (84%), `_corpus_step` 1.14 s over 2,360 calls, which reproduces the Note above to the call. After: **1.70 s** unprofiled, 2.04 s under `cProfile`, no numpy forward pass, `_corpus_step` 1.13 s over 2,360 calls, so 5.5 times faster and the Cython E-step is now two thirds of a round. Both rank the same 20 moves with bit-identical totals.
  > **Done:** `search_round_profile.py` tracked; ADR 0024's Evidence carries the reproduced profile and the measured round after §2, its estimate and Open item are gone, and its Positive and Negative consequences state the measured figures (the "few tenths of a second" per trial was an overestimate: about 0.085 s).
- [x] Measure whether a search on `backend="torch"` actually leaves the serial path on the tracked fixtures (ADR 0024 Open), rather than inferring it from ADR 0023's step sensitivity, and record the finding in ADR 0024 in place of that Open item.
  > **Note:** Probed first: torch EM on the CPU costs 248 ms/cycle on `m001_0005_005` and 183 on `m008_0001_008` against Cython's 0.5, over the corpus as one record, and after 50 cycles `m008`'s description length already differs from Cython's in its last printed digit. `_search` takes no `device=`, so a torch search runs its E-step on the CPU whatever GPU is present.
  > **Q:** How much should the measurement cover, given a torch round costs its trials times at least 200 EM cycles at ~200 ms each: one round compared candidate by candidate on each saved incumbent plus the 3-round trajectory from the one-state start, the trajectory only, the saved-model rounds only, or add `device=` to `_search` first?
  > **A:** Rounds plus the short trajectory: one `_suggest_move` round on `m001_0001_001`, `m001_0005_005` and `m008_0001_008` (1, 5 and 8 states) compared candidate by candidate, and `_search` from the one-state start for the 3 rounds the Lush log records, both on `backend="cython"` and `"torch"` with `score_backend="cython"`. About 1-1.5 h in the background. No `device=` on `_search`.
  > **Note:** Ran in two parts: run 1 covered the 1- and 5-state rounds, then the script was extended with EM-cycle sums, a per-candidate "which backend scored lower" column and `--models`, so run 2 covered the 8-state round and the trajectory. Both logs are kept.
  > **Note:** Result: torch never left the serial path. All 66 candidates chose the same `d`, took the same EM cycles (400/400, 2,340/2,340 and 8,500/8,500 summed per round) and scored bit-identical totals, so rankings, winners and the 3 search moves agreed, including at `m008` where the winner led by 0.0513 bits. Unrounded data lengths differed by at most 1e-11 bits: the quantization at `d` absorbs it, which is why `data_bits` differs and `total_bits` does not. Torch on the CPU was 350-400 times slower (124, 742, 2,768 s and 802 s against 0, 2, 8 and 2 s).
  > **Note:** The search reaches a GPU through numba-cuda (`backend="cuda"`, `score_backend="cuda"`), but not through torch: `device=` is `baum_welch`'s alone and neither `_trials.py` nor `_search.py` has it, so a torch search runs on CPU threads. Deferred by the Q&A below rather than plumbed.
  > **Q:** What should happen about a torch search being unable to select a GPU device before revision 04's release: a `DEFERRED.md` trigger plus documentation, plumbing `device=` now, or plumbing it and measuring on the L4?
  > **A:** The `DEFERRED.md` trigger and documentation. At `S <= 8` `cuda` costs about 500 ms per corpus forward pass, so the plumbing would be unexercised, and `_search` is private, so adding it later breaks no published signature.
  > **Q:** Where should the benchmark be recorded, given the numbers go stale but the tradeoffs do not?
  > **A:** A new `docs/benchmarks/` area, with a README saying it holds dated observations rather than contracts, and a page carrying both runs' verbatim logs in full; `docs/api/` keeps the durable guidance and points at it. Timings cannot live in `docs/api/` code blocks, which `tests/test_api_docs.py` executes. The scripts are not copied there: the page names each one's path and the SHA-256 of the version that produced its log, as `FORMALIZATION.md` does for its kernel.
  > **Done:** ADR 0024's Open item is answered: §4's first bullet now states the measurement and the CPU cost, Evidence carries the table, and a Resolved entry explains why the tolerance is invisible to the criterion. `torch_search_path.py` is tracked.
- [x] Record the backend benchmark under a new `docs/benchmarks/`: a `README.md` saying the area holds dated observations on one host rather than contracts, and `hmm-backends.md` carrying the tradeoff tables and **both runs' verbatim logs** of `torch_search_path.py`, with each log's script named by path and by the SHA-256 of the version that produced it (as `FORMALIZATION.md` does for its kernel), plus the round profile's before/after figures. Timings stay out of `docs/api/`, whose code blocks `tests/test_api_docs.py` executes against pasted output.
  > **Note:** Run 1's version of `torch_search_path.py` was never on disk after the edit, so only run 2's hash (`f239a501...`) is recorded; the page says so rather than implying one script produced both logs. `search_round_profile.py` is `ef4ed521...`.
  > **Note:** `tests/test_api_docs.py` reads `docs/api/*/*.md` and `packages/*/README.md` only, so nothing executes the benchmark page's blocks, which is why the logs can be pasted verbatim.
  > **Done:** `docs/benchmarks/README.md` states the area is dated observations with the SHA-256 staleness rule; `hmm-backends.md` carries the contract/measurement split, a five-row guidance table, both measurements with their tables, both runs' logs verbatim, and what the numbers do not say. Every relative link resolves. The `DEFERRED.md` pointer is left for goal 7, which writes that entry.
- [x] Document it in `docs/api/hmm/baum_welch.md` with executed output (ADR 0013): the new choice if any, that python/cython/cpu_parallel/cuda return identical results and differ only in speed, that `torch` is held to a tolerance and is the only phase that could change a search's path, though on the tracked fixtures it did not, and that a torch search runs on the CPU while `cuda` reaches the device, and that Cython is the fastest phase at the sizes measured (`S <= 8`; `cpu_parallel` 18-24 ms and `cuda` about 500 ms per corpus forward pass, `total_forward_cost.py`). Carry the same guidance forward to whatever page documents the search when it becomes public.
  > **Note:** The examples are executed by `tests/test_api_docs.py`, which matches a pasted `SomeError: message` line against the exception actually raised, so the refusal example pins the argument label: relabelling it plain `"baum_welch"` fails the doc test. Confirmed by perturbing that line, which failed the run, then restoring it.
  > **Note:** The torch pairing example asserts only `mixed.cycles == result.cycles`. On this corpus the last description length happens to match exactly, but torch's parameters agree only within a tolerance, so pasting that equality would pin a coincidence.
  > **Note:** The search is still private, so the guidance is carried forward by a sentence saying it applies unchanged when the search becomes public, rather than by creating a page for an unexported function.
  > **Done:** `baum_welch.md`'s signature gains `score_backend`, a speed paragraph states which phase is fastest at which size and points at the benchmark page, and a new "The convergence check has its own backend" section documents the keyword with three executed examples: identity across phases, the torch refusal with its exact message, and torch EM checked by a compiled pass. `backends.md` notes that one call can name two backends. `tests/test_api_docs.py`: 11 passed.
- [ ] Add the `DEFERRED.md` trigger "a topology search too slow to run at the model sizes in use" with ADR 0024 §3's content: the axis is trials within a round, not kernel cells; results are bit-identical to the serial run when gathered in enumeration order, because seeds are keyed by trial identity `(1, r, s, t)` and the ranking is a stable sort whose order is the tie-break; the hazards are processes × `cpu_parallel` threads oversubscribing and processes sharing one GPU, so at small `S` the shape is processes over serial `cython` kernels; and the test owed is a parallel `SearchResult` `==` the serial one on a seed that exercises ties.
  > **Note:** A second entry belongs here, from goal 5: trigger "a search at model sizes where a GPU E-step beats `cython`". The search reaches numba-cuda through `backend=`/`score_backend=` but cannot reach a torch device, because `device=` is `baum_welch`'s alone and neither `_trials.py` nor `_search.py` carries it. The change is `device=` through `_try_split`, `_try_merge`, `_suggest_move` and `_search`; it is not made now because at `S <= 8` `cuda` costs about 500 ms per corpus forward pass against Cython's fraction of a millisecond, so the plumbing would be unexercised; the test owed is a keyword-forwarding spy like `score_backend`'s, and `_search` is private, so adding it later breaks no published signature.
- [ ] Add a qualifying note under `HMMLIB-ACCOUNT.md` §12 pointing at ADR 0024's profile, and a pointer from ADR 0023 §7 to ADR 0024 §2, which extends it to the convergence check.
- [ ] Sync `core.md` (0024's status once Accepted, the new script, test counts, `baum_welch`'s signature if it changes) and rebuild `AGENTS.md`.
  > **Note:** The ADR count of 24 and 0024's summary already landed on `main` in `d0cc0fe`, with the record itself, so what remains is keeping that summary true to the decision.
