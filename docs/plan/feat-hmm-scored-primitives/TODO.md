# feat/hmm-scored-primitives

**Status**: active
**Created**: 2026-09-17
**Subgoal**: Port the scored primitives — `docs/plan/TODO.md`, revision 04-hmm-v0.3.0

## Goals

- [x] Specify what `try-split`, `try-merge` and `suggest-*` do before writing code
  - [x] Transcribe `try-split` (`hmm-trainer.lsh:749-770`) and `try-merge` (`:858-881`): what is re-converged, with which stopping rule, and how `d` is chosen per trial
    > **Note:** The two are identical apart from the surgery line. Each one: (1) applies `split-state` or `merge-states` to the working `params`; (2) runs `update-data-p`, the forward pass whose unrounded bits become `data-p`; (3) sets `last-saved-d` to the incumbent's `d` and zeroes the cycle counters; (4) runs `run-converge`; (5) runs `suggest-d`, which is `minimize-int` over the total on `[1, 10000]` starting **from 3821 on every trial**, never warm-started from the incumbent's `d`; (6) runs `keep-d`; (7) runs `update-all ()`, which recomputes entropies, `data-p`, the **Viterbi path**, the rounded matrices at `d`, and `data-dl`/`model-dl`/`total-dl`. Nothing is returned: the result is `total-dl` together with the converged `params`, left in trainer state for the caller to copy.
    > **Note:** `run-converge` is the rule `baum_welch` already has, measured on `data-p`, which is the unrounded corpus length and so independent of `d`. `old-data-p` starts at the *candidate's* starting value, and batches run 10 cycles each; 3 consecutive changes under `0.1 × *factor*` (with `*factor*` = 1) stop it. So **the original's floor is 30 cycles**, it has no cap, and a split that starts at the incumbent's likelihood meets that floor exactly, which is ADR 0022 §4's problem.
  - [x] Transcribe `suggest-split`, `suggest-merge` and `suggest-move` (`:952-1073`): trial counts, draw order, ranking and tie-breaking
    > **Note:** **`suggest-split`** loops `s = 0..S-1` ascending, with `2 + int(*split-trials-per-state-p* · π_s)` trials per state. The constants are `*min-split-trials*` = 2 and `*split-trials-per-state-p*` = 0, with the comment "20, or even 50 if we want to be thorough", so every state gets 2 trials as shipped. `π_s` is read from `:model:state-p`, the incumbent, which stays fixed through the round because `reset-model` restores `params` (including `d`) from the model after every trial. Random draws happen only inside `split-state`, from one global stream, in (state, trial) order; EM and `suggest-d` are deterministic.
    > **Note:** **`suggest-merge`** tries every pair `a < b` in lexicographic order, one trial each, with no randomness: `S(S-1)/2` full re-convergences, each followed by a Brent search. **`suggest-move`** runs all merges and then all splits against one shared best, so a tie *between kinds* goes to the merge. At `S = 1` the merge loop is empty.
    > **Note:** **Ranking.** Each `suggest-*` keeps only the argmin under a strict `<`, so ties go to the first candidate in iteration order. `best-total-dl` starts at `1e100`, the same value as the impossible-model sentinel, so an impossible candidate can never be selected. **If every candidate is impossible, the `best-*` slots stay `()` and `idx-copy` fails**, so the original has no "no move possible" path. The winner is installed unconditionally, whether or not it beats the incumbent: accepting it is the user's `keep-model`, and rejecting it is `reset-model`. Every trial also appends its total to `<model>.trials.txt`.
  - [x] Mark every point where ADR 0022 or an earlier decision makes the port depart from the original
    > **Note:** **Departures.** (1) **The candidate**: `_split_state` follows ADR 0022 §1-3, and `_merge_states` fixes `:262`, weights by exact stationary mass, and refuses two transient states. The original's search results are therefore not reproducible. (2) **The EM floor**: ADR 0022 §4 needs one above the original's effective 30 cycles (the ADR measured 200 forced cycles), and `baum_welch` has no parameter for it today; goal 2 decides the mechanism. (3) **Unrounded length**: ADR 0022 §5 reports `_corpus_description_length` beside each total, where the original logged the total alone. (4) **Randomness**: one `Generator` per (round, state, trial) replaces the global stream (ADR 0022 Resolved). (5) **Impossible models**: `+inf` replaces `1e100`. Since results are ranked rather than reduced to an argmin, "every candidate impossible" becomes a well-defined result instead of a crash. (6) **Ranked results**: all candidates are returned rather than the winner being staged into mutable state. First-wins is kept as a **stable sort on the total over iteration order** (merges before splits in `suggest-move`), so that tie-break stays contract. (7) **No `reset-model`**: under ADR 0017 every candidate is built from the frozen incumbent, so a reset is structural. (8) **A refused merge**: `_merge_states` raises on two transient states, where the original built an all-zero row, so goal 3 decides whether such a pair is skipped or ranked as impossible, together with the merge-weight question. (9) **Dropped per trial**: the Viterbi path and entropy refresh (annotation only under ADR 0017), the `trials.txt`/`printf` output (training-log subgoal), and the `dirty` guards with the `-with-update` GUI wrappers (`hmm-trainer-view.lsh` migrates nowhere).
    > **Note:** **Faithful on purpose.** `run-converge`'s rule, and its quantity being the same function as the data half. `_suggest_d`'s Brent search from 3821 on every trial (a global scan is the loop's decision). A score that is only ever obtained by calling the total. All-pairs merging, including its quadratic cost. And no verdict against the incumbent, since the acceptance rule belongs to the search loop.
    > **Note:** **A stale paragraph in ADR 0022, fixed on this branch.** Its Consequences bullet "The draw order changes" said a split draws "one `u_j` per live predecessor plus one fibre per live outbound arc". That contradicted its own Resolved section and `_split_state`, which draw for *every* predecessor and destination, so that the draw count depends only on `S`. The bullet now says so.
- [x] Port `try-split`
  > **Q:** ADR 0022's "200 forced cycles" was measured by chaining two `baum_welch` calls (`split_symmetry_breaking.py:126-127`: a forced run under `max_cycles`, then the default stop), and `baum_welch` has no floor today. How does the minimum EM budget reach `run-converge`? (A) chain the two calls inside `_try_split`, matching the evidence but splicing two histories and two log blocks, with a real floor of `min_cycles + 30`; (B) add a keyword-only `min_cycles: int = 0` to the public `baum_welch`, looping `while unchanged < patience or cycles < min_cycles`; (C) as B, but through a private path only `_try_split` reaches.
  > **A:** B. The floor is what ADR 0022 §4 says a split needs, not a quirk of search, and a user re-training a split by hand needs it as much as the search loop does. With a default of 0, every existing call is unchanged bit for bit. Two costs are accepted: an executed example in `docs/api/hmm/baum_welch.md`, and the floor rounding up to a multiple of `batch_cycles`.
  > **Q:** Is the rest of the signature right: `_try_split(params, records, state, *, rng, min_cycles, backend="python", batch_size=None)` returning `params`, `d`, `total_bits`, `data_bits` (unrounded, §5), `cycles` and `converged`; the caller passing the trial's own `Generator`; `min_cycles` required with no default; `d` from `_suggest_d`; the total only from `_total_description_length`; and a new private module `_trials.py`?
  > **A:** Yes, as proposed.
  - [x] Decide the signature: the minimum EM budget (ADR 0022 §4) as a parameter, and one `Generator` per (round, state, trial)
    > **Note:** `rng` is the trial's own `Generator`. Building one per (round, state, trial) stays the search loop's choice under ADR 0022 Resolved, so `_try_split` takes no seed and no trial numbers. `min_cycles` has no default because ADR 0022 Open leaves its size to the search loop, and a hidden 200 would decide that question by accident. `_trials.py` keeps `_topology.py` candidate-building only.
  > **Note:** `baum_welch(..., min_cycles=)` landed first, with six tests and an executed example in `docs/api/hmm/baum_welch.md`. `_near_saddle(1e-5)` shows both the failure and the fix: the default rule stops at cycle 8, 400 bits above the optimum, while `min_cycles=10` puts the first allowed check on a changed batch, so the run escapes and stops at 30 with 0 bits. Reverting the loop condition failed three of the six tests.
  - [x] Report unrounded data description length beside the total (§5), scoring through `_total_description_length`
    > **Note:** `_trials._try_split` returns `TrialResult(params, d, total_bits, data_bits, cycles, converged)`. `data_bits` is `baum_welch`'s last history entry, which its docstring promises is the reference forward pass on every backend. `total_bits` costs one more evaluation than Brent already made, because reading the score out of `_minimize_int` would take a value in which `+inf` has been mapped to `1e100`.
  - [x] Tests
    > **Note:** `tests/test_trials.py` has 9 tests, pinning the composition rather than re-testing its parts. **The first floor of 20 was vacuous**: the case converges in 180 cycles without a floor, so dropping `min_cycles` passed every test. It is now 205, and a test asserts the default run stops earlier. Three defects were each caught: the floor dropped (3 failures), the rounded data half reported as `data_bits` (1), and the total assembled from its halves (1). Suite: 2920 passed.
  > **Done:** `baum_welch(..., min_cycles=)` is public and documented, and `_trials._try_split` implements `try-split` with ADR 0022 §4 and §5, scored through `_total_description_length`.
- [x] Port `try-merge`
  > **Q:** How does `try-merge` weight a pair's outbound arcs? (A) keep stationary mass, as `merge-states` does, with `suggest-merge` filtering out pairs of two transient states using `closed_classes`; (B) weight by E-step occupancy through a `weights=` argument on `_merge_states`, computed once per round; (C) occupancy only when the pair includes a transient state.
  > **A:** A. `_try_merge` stays a thin wrapper like `_try_split`, and `_merge_states` keeps exact lumping. A pair of two transient states is never tried, and is reported as skipped, not as impossible.
  - [x] Decide the merge weights: reweight from E-step counts, or filter transient pairs with the recurrent mask
    > **Note:** **Measured before deciding.** On all three tracked models, under both flat and per-record retraining on the 200 records, every state is recurrent. Stationary mass and E-step occupancy agree to within 0.003 (`m008` per-record, state 5, which starts 63% of records). The corpus writes `begin`/`end` into the stream, so each record's end leads back to a start and the chain behaves like one long run. An occupied transient state needs a corpus without such markers, and no fixture has one.
    > **Note:** **The cost A accepts, and when to revisit it.** An occupied transient state merged with a recurrent one contributes nothing to the outbound arcs, so an arc only it had stays dead, since EM never revives a zero. Revisit B once a corpus shows trained models with occupied transient states. B is then a `weights=` argument on `_merge_states` plus one E-step per round.
  - [x] Implement and test
    > **Note:** `_try_merge(params, records, first, second, *, min_cycles=0, backend, batch_size)` and `_try_split` now share `_re_converge_and_score`, as the Lush methods share every line but the surgery. The merge's floor defaults to 0, the original rule, because a merge does not start at the incumbent's likelihood. 7 tests are in `test_trials.py`, including argument-order symmetry, so ranking can enumerate unordered pairs, and the refusal of two transient states. Two defects were each caught: ignoring the floor (only the spy sees it, since the default is 0) and skipping re-convergence. Suite: 2927 passed.
    > **Note:** **For goal 4: a merge can make a record impossible, and `_try_merge` then raises `ImpossibleSequenceError` from `baum_welch` rather than returning `+inf`.** When both states carry stationary mass, every live arc and emittable symbol survives the weighted average, so paths are preserved; none of the 38 fixture pairs made the corpus impossible. When one state is transient, its outbound arcs get weight 0 and a path through them dies. The ranking must therefore catch that error and rank the candidate `+inf`, or check the corpus length before re-converging.
- [x] Port `suggest-split`, `suggest-merge` and `suggest-move` as ranked results
  > **Q:** The shape: each `suggest-*` returns `list[Move(kind, states, trial, result, total_bits)]`, enumerated in the original's order (states then trials; pairs lexicographic; merges before splits) and stably sorted by `total_bits`, so `[0]` is the original's strict-`<` winner, ties included. It uses the shipped trial-count constants (2 and 0) as defaults and gives no verdict against the incumbent. Within that: (1) how does an impossible candidate enter the ranking: (a) catch `ImpossibleSequenceError` and record `result=None, total_bits=inf`, or (b) drop it? (2) A pair of two transient states: (a) leave it out, filtered by `closed_classes` before trying, or (b) list it with `nan`? (3) Each split trial's generator: (a) take the round's `SeedSequence` and build `SeedSequence(entropy, spawn_key=key + (state, trial))`, or (b) take a factory `rng_for(state, trial)`?
  > **A:** 1a, 2a and 3a, with the shape as proposed. Impossible candidates stay visible and sort last, so "every candidate impossible" is an all-`inf` list rather than the original's crash. A transient pair is not a candidate. Each generator depends only on the round and the trial's identity, as ADR 0022 Resolved requires.
  - [x] Handle impossible (`+inf`) candidates by comparing totals, never subtracting them
    > **Note:** `_trials.py` gains `Move` and `_suggest_split`/`_suggest_merge`/`_suggest_move`. Each enumerates candidates in the original's order and returns them through one `_ranked`, which is `sorted` on `total_bits` alone: Python's sort is stable, and it compares totals without ever subtracting them. `_attempt` turns an `ImpossibleSequenceError` into `Move(result=None, total_bits=inf)`. A candidate whose converged total rounds to `+inf` at its `d` already carries `+inf` from `_total_description_length`. Pairs of two transient states are filtered by `closed_classes` before any trial runs. The shipped constants are module names: `MIN_SPLIT_TRIALS = 2` and `SPLIT_TRIALS_PER_STATE_P = 0.0`.
  - [x] Tests, including the ranking contract
    > **Note:** 16 tests. The contract is checked on constructed `Move` lists under `np.errstate(all="raise")`: ties keep enumeration order, a merge beats a split on a tie, `inf` ties with `inf`, and an all-impossible round is an ordinary list. Spies stand in for the trials to check order, trial counts, the pass-through arguments and **identity-keyed generators**: trial `(s, 0)` draws the same value whether each state runs 1 trial or 3, and every draw matches `SeedSequence(entropy, spawn_key=key + (s, t))`. Three run real trials end to end, on `_transient_start()` (a transient start whose only arc emits `a`, so merging it kills that path), on a 3-state merge round, and on a split rebuilt from its identity alone. Six mutants were each caught: splits enumerated before merges, a tie-break by kind, `ImpossibleSequenceError` not caught, generators keyed by execution order, the transient filter with `or`, and trial counts ignoring `state_p`. Suite: 2943 passed.
- [x] Measure and record
  - [x] Measure the all-pairs merge cost against model size
    > **Note:** `.scratch/hmm-lush/measurements/merge_round_cost.py` runs one `_suggest_merge` round on `cython` over `set11a_dInt` (1449 symbols over 25) at `S = 2..16`, on a 4-vCPU x86_64 host. It then times the same pairs phase by phase, and the phase sums match the round to within 1%. Rounds took 0.7, 7.9, 43, 97 and 200 s for 1, 6, 28, 66 and 120 pairs, at **1.3 to 1.7 s per pair**. The log-log slope is 1.17 against the pair count and 2.67 against `S`; the small-`S` points steepen the second, since `S(S-1)/2` grows faster than `S²` there. **So the quadratic cost is the trial count.** A trial costs about the same at any size tried, and anything that prunes pairs before trying them (the deferred alignment seed) saves in direct proportion.
    > **Note:** **Scoring, not EM, is most of a trial.** `_suggest_d` takes 53-62% at `S ≥ 4` (79% at `S = 2`), `baum_welch` on `cython` 35-44%, the final total about 3%, and building the candidate 0.0%. `_suggest_d` has no `backend=`: its roughly 20 Brent evaluations each run the numpy forward pass, while EM already runs compiled. Routing the forward pass inside `_total_description_length` through a compiled kernel would roughly halve every trial, independently of pruning. That is a finding, not work for this branch: it belongs with the search loop's `d` decision, which already has to choose between Brent and a global scan.
  - [x] Update `core.md`
    > **Note:** Each commit's docs sync had already kept `core.md` current: the fourteenth module, `min_cycles`, `try-merge`'s weights and the rankings, with test counts checked by collecting the tests. This subgoal added the merge-round measurement to the `_trials.py` paragraph and `merge_round_cost.py` to the measurement scripts.
