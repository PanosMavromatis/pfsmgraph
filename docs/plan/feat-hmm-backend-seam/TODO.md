# feat/hmm-backend-seam

**Status**: active
**Created**: 2026-09-14
**Subgoal**: Write the runtime backend-selection ADR and build the seam it decides — `docs/plan/TODO.md`, revision 03-hmm-v0.2.0

## Goals

- [x] Write ADR 0021 on runtime backend selection
  > **Q:** How should a caller choose the backend: a per-call keyword only, a keyword plus a module-level default (`set_backend` / `use_backend` context), or backend objects?
  > **A:** A per-call keyword only, `viterbi(params, record, *, backend=...)`, and the same keyword on the training entry points when they become public. No module state: the choice is visible at every call site, which follows `encode(on_unknown=...)` and `rand_p_vector`'s required generator, and ADR 0003 tests pass the fixture value straight through.
  > **Q:** What should the public backend names be?
  > **A:** The registry's names as strings, `"python" | "cython" | "cpu_parallel" | "cuda"`, typed as a `Literal` and validated before any work. They are the same vocabulary as the session header and `PFSMGRAPH_REQUIRE_BACKENDS`; `torch` becomes a fifth value.
  - [x] Settle the public shape: a per-call `backend=` keyword, a module-level default, or both; and the backend names
  > **Q:** What should `backend=` default to: `"python"`, `"cython"`, or an `"auto"` value resolving to the best available?
  > **A:** `"python"`. The phase-1 reference is present in every install and every wheel and is ADR 0002's oracle, so acceleration stays opt-in (ADR 0004) and a call never changes behaviour because an extra or a device appeared. `"auto"` was declined because "best" depends on `S`, `N` and the host (phase 3 is 853x slower than phase 2 at `S = 5` on one host and faster at `S = 64` on another), and because it would let a result's backend, and after `torch` its last ulps, depend on the environment.
  > **Q:** What happens when a caller explicitly requests a backend that is unavailable here?
  > **A:** Raise before any work, with a `BackendUnavailableError` (an `ImportError` subclass) naming the reason and the remedy (the extra to install, or "no CUDA device detected"). No fallback and no opt-in fallback keyword: ADR 0011's strictness applies, and "silently ran on the CPU path" is ADR 0003's own example of work that merely looks fine. A name that is not a backend at all is a plain `ValueError`.
  > **Note:** at runtime a missing `cython` extension is a legitimate absence, not the broken working copy ADR 0003 makes a hard test failure: `meson.options`' `compiled=false` already builds a pure wheel, and a platform with no binary wheel installs one. The ADR has to keep the two policies apart. The test suite still escalates an unbuilt extension; the API reports it as unavailable.
  - [x] Settle the default backend and what an unavailable requested backend does (raise vs fall back), against ADR 0011's strictness position
  > **Q:** Where should the backend registry live, so the runtime and the repo-root test header read one source: in `hmm` with the root keeping only the test policy, at the root with a mirror in `hmm`, or in `dataseq` as a family registry?
  > **A:** In `hmm`. A private, shipped `pfsmgraph/hmm/_backends.py` holds the table, keyed by algorithm and then backend name: the kernel module, what an absence needs (nothing, the compiled extension, numba, a CUDA device) and the extra that supplies it. The runtime resolves it lazily. The repo-root `_backends.py` keeps only ADR 0003's policy (header, skip versus escalation, `PFSMGRAPH_REQUIRE_BACKENDS`) and reads `hmm`'s table; `align` will ship its own table, and the root will read both, so no distribution imports another's registry. A mirror was declined as the drift ADR 0003 exists to prevent, and a `dataseq` registry because the base layer has no DP and would need a release for every new row.
  > **Note:** today's registry is one algorithm, a row per Viterbi phase. `forward_backward` is registered with `dp-compile` but absent from `BACKENDS`, so the header cannot show it; keying by algorithm fixes that. `dp-compile.toml`'s `[backends] registry = "_backends.py"` must move with the table, or phase skills will add rows where nothing reads them.
  - [x] Settle where the registry lives, so the runtime and the repo-root test header read one source
  > **Q:** Reviewing the draft, its Open section left public enumeration undecided. Should it be settled, and in what shape: a status per backend, available names only, or keyed by the callable?
  > **A:** Settled, as a status per backend: `pfsmgraph.hmm.backends("viterbi")` returns frozen `BackendStatus(name, available, reason)` rows in phase order. Unavailable backends are included, with the same reason `BackendUnavailableError` carries. The key is the public call's name, never a private kernel's. It probes once per process and caches the result. The repo-root header is built from it plus the private "absence attributable to" field, so the header and the API cannot disagree. Recorded as ADR 0021 §4.
  > **Note:** the draft's second Open item, batching and device placement, stays open. It belongs to the master plan's "Batch the trainer over sequences" subgoal, which now carries a note saying so, including whether `viterbi` gains a batched form there, since that subgoal is worded for the trainer only.
  > **Done:** [ADR 0021](../../design/adr/0021-runtime-backend-selection.md) records all four decisions: a per-call `backend=` string, a default of `"python"`, `BackendUnavailableError` with no fallback, the table in `hmm` with the test policy at the root, and public `backends(name)`. It is indexed in `adr/README.md`, and ADR 0003's Resolved section points at it.
- [x] Build the seam in `pfsmgraph-hmm`
  > **Q:** ADR 0021 §3 has the root header take availability from public `backends()`, but §4 keys `backends()` by public call names and Consequences promises a header row for private `forward_backward`. How is that reconciled: a private status function behind `backends()`, a header of public calls only, or `backends()` accepting private keys?
  > **A:** A private `_status(algorithm)` probes and caches any row in the table. Public `backends(name)` is `_status` restricted to public call names, and `backend=` resolution reads the same cache. The root header calls `_status` for every algorithm, so there is still one probe and `forward_backward python ✓` appears. §3's wording is amended in goal 5.
  > **Note:** EM has no row of its own. `_baum_welch.py` is not a DP kernel and reaches forward-backward through `_corpus_step`, so the table holds `viterbi` (four rows) and `forward_backward` (one), and EM's backend will be whatever that second row resolves to once a training call is public.
  - [x] `viterbi` dispatches over all four phases through it
    > **Note:** through the public call, 295 of 295 generated models (the `_generated_models` shapes, `S` up to 12) decode to identical states and `total_bits` on all four backends on this host. The impossible-sequence message still comes from the Python `_dead_symbol`, since the failure belongs to the model rather than the backend.
  - [x] Export `backends(name)`, `BackendStatus` and `BackendUnavailableError` from `pfsmgraph.hmm`, with one per-process probe cache shared by `backends()` and `backend=` resolution
    > **Note:** simulated absences by planting `None` in `sys.modules`: a missing extension reports the pure-wheel remedy, missing numba the `cpu-parallel` extra, and missing numba-cuda the `gpu` extra, and `viterbi(backend="cpu_parallel")` then raises `BackendUnavailableError` with the same text. A first draft blamed any `ImportError` from the Cython row on a pure wheel. Now only the extension module itself being absent does, and anything else reports the real error, since that is a stale build. `import pfsmgraph.hmm` loads no numba.
  - [x] Register forward-backward and EM kernels in the table at phase 1 only; they stay private, so no public `backends()` key until a training call is public
    > **Note:** `_em` and `_corpus_step` still call `_forward_backward` directly. The row exists so goal 3's header can report it, and nothing resolves through it yet.
  > **Note:** `docs/api/hmm/README.md` pastes `pfsmgraph.hmm.__all__`, so exporting three names failed `test_api_docs.py`. The pasted list and "four names" were updated here to keep the suite green (530 passed); the rest of the API documentation is goal 5's.
  > **Done:** a shipped, private `pfsmgraph/hmm/_backends.py` holds the table (four `viterbi` rows, one `forward_backward` row), a locked once-per-process probe, remedy text derived from the failed import, `_status`, `_resolve` and public `backends()`. `viterbi` takes keyword-only `backend=` and resolves it before any work. `BackendStatus`, `BackendUnavailableError` and `backends` are exported, and the module is listed in `meson.build`. Tests for the seam are goal 3's.
- [x] Parameterise the ADR 0003 suites over the seam
  > **Q:** How far should `test_viterbi.py`'s non-shared section fold into the backend-parameterised shared tests: a full fold that keeps a kernel section, parameterising while keeping the phase sections, or a full fold with no kernel section?
  > **A:** Full fold, keeping a kernel section. A `backend` fixture in a new package-level `tests/conftest.py` parameterises every shared test that decodes; an unavailable backend stays a parameter and skips with its `backends()` reason. The per-phase duplicates (saved decodes, seeding divergence, ties, frozen arrays, shape and block boundaries) become shared tests, plus one generated-models equivalence test against `backend="python"`. Below the line remain: a kernel-contract section parameterised through `_resolve` (a kernel returns `inf` rather than raising, which is what keeps CUDA a transliteration), the phase-specific tests (cpu_parallel thread counts, cuda block size and empty record), and the libm discrimination test.
  > **Q:** What should the rebuilt one-line session header look like, now that the table is keyed by algorithm?
  > **A:** Grouped by algorithm, still one line: `backends: viterbi python ✓ · cython ✓ · cpu_parallel ✓ · cuda ✗ (no CUDA device detected) | forward_backward python ✓`. `PFSMGRAPH_REQUIRE_BACKENDS=cuda` requires `cuda` wherever an algorithm has that row, and a name no algorithm has is rejected.
  > **Note:** the package's tests for `backends()` cannot be called `test_backends.py`. That basename is taken by the repo-root `tests/test_backends.py`, and with no `__init__.py` in either directory pytest's default `prepend` import mode stops with "import file mismatch". They go in `test_backend_selection.py`.
  - [x] Shared Viterbi tests run once per available backend through the public API
    > **Note:** `test_viterbi.py` went from 93 tests to 242, 55 per backend plus the backend-specific ones, and still runs in about 5 s. The full suite is 679 (was 530). The oracle decodes each saved model once per backend through a cached `_oracle(model, backend)`, and a separate `saved` fixture serves the two tests about the files themselves, so they do not run four times.
    > **Note:** mutation check: changing phase 3's recurrence comparison from `<` to `<=` fails four tests, all on `cpu_parallel`: the shared tie test, the generated-models equivalence test, the tie-in-bits test, and the thread-count tie test. Before this goal only the last of those ran on phase 3.
  - [x] Fold `test_viterbi.py`'s labelled non-shared section into them
    > **Note:** removed as duplicates: each phase's saved-decode, seeding-divergence, tie and frozen-array copies, now shared tests or one parameterised equivalence test against `backend="python"` (200 generated models plus sizes 1, 63, 64, 65 and 130). Kept below the line: three kernel-contract tests through `_resolve` (returns `inf`, reads frozen arrays, reproduces the seeding defect given the original's seed), two phase-3 thread tests, two phase-4 launch tests, and the libm discrimination test. The CUDA empty-record test now proves no launch happens by setting `_step` to `None`; a one-symbol record then fails with `TypeError`, so that check can tell the difference.
  - [x] Rebuild the repo-root `_backends.py` on `backends()` plus the private absence field (no table of its own); header rows per public call; rewrite `tests/test_backends.py`; move `dp-compile.toml`'s `[backends] registry`
    > **Note:** the root module now holds no table: `detect()` reads each module in `TABLES` through its `_status`, and escalates when the absence is attributable to nothing or to the compiled extension (`ESCALATED_NEEDS`). The live header reads `backends: viterbi python ✓ · cython ✓ · cpu_parallel ✓ · cuda ✓ | forward_backward python ✓`. `PFSMGRAPH_REQUIRE_BACKENDS` requires a name on every algorithm that has it, and names the algorithm when escalating. `tests/test_backends.py` swaps synthetic rows into the real `_TABLE` with an empty cache, so its cases exercise the real probe and reason text; a new test checks that the header's reasons equal `backends()`'s. Root tests: 50 (was 46); suite: 683.
    > **Note:** `dp-compile.toml`'s `registry` is a literal path to `hmm`'s table, since the manifest documents `{package}` substitution only for `[phases]`. A comment there says the rows are `_Row` entries under `_TABLE`, not the plugin template's flat `Backend` tuple, and that `align` will need its own entry.
  - [x] Test `backends()` itself: phase order, unavailable rows included, `reason` identical to `BackendUnavailableError`'s message, one probe per process
    > **Note:** `packages/pfsmgraph-hmm/tests/test_backend_selection.py`, 25 tests. Validation-before-work checks pass `None` for the model and record, so a check that ran late fails with `AttributeError`. Absences are simulated by planting `None` in `sys.modules` and forgetting any already-imported kernel. A synthetic stale extension on `tmp_path` checks that only the extension itself being absent is described as a pure install. One probe per process is counted by replacing `importlib` inside `_backends`, including 8 threads through a barrier. That the package imports no backend dependency is checked in a fresh interpreter.
    > **Note:** mutation check, sources restored after each: dropping the stale-build distinction fails 1 test, dropping the cache fails 2, and resolving `backend=` after `record.codes` fails 8.
  > **Done:** every shared Viterbi test runs on every backend through a `backend` fixture (242 tests, 55 per backend), with one exact equivalence test against `backend="python"`; the per-phase duplicates are folded in, and a smaller kernel-contract and phase-specific section remains below the line. The repo-root `_backends.py` holds no table and reads `hmm`'s through `_status`, and the header is grouped by algorithm. `backends()` and `backend=` have 25 tests of their own. Suite: 708 passed.
- [ ] Restore the accelerator extras (`cpu-parallel`, `gpu` with the `numpy<2.5` cap)
- [ ] Update the docs: `docs/api/hmm/`, `core.md`, the ADR index, and the `DEFERRED.md` entries
  - [ ] Document `backends()` and `backend=` in `docs/api/hmm/` with host-independent executed examples (e.g. the `python` row, or an unknown-name `ValueError`), since the ADR 0013 verifier compares pasted output and availability varies by machine
